# Session Manager

> Умное отслеживание сессий для разработчиков — никогда не теряйте контекст при переключении между проектами

## 🎯 Что делает его особенным?

Session Manager — это не просто еще один трекер времени. Это ваш компаньон по контексту проекта, который:

-   **💾 Сохраняет контекст**: Автоматически запоминает, что вы делали и что планируется дальше
-   **⏱️ Отслеживает время**: Простое отслеживание времени по сессиям для нескольких проектов
-   **🔗 Глубоко интегрируется**: Работает с Git, pytest и GitHub Issues
-   **🏠 Остается локальным**: Все данные хранятся локально — полная конфиденциальность
-   **🚀 Работает везде**: С git или без — встроенная поэтапная деградация функционала
-   **📝 Автоматически генерирует PROJECT.md**: Возвращайтесь к работе мгновенно с автоматически создаваемыми файлами контекста

## Установка

```bash
pip install -e .
```

После установки команда `session` будет доступна глобально.

## Требования

-   Python 3.8+
-   **Опционально** (для расширенных функций):
    -   `git` — для интеграции с Git
    -   `pytest` — для отслеживания статуса тестов
    -   `gh` — GitHub CLI для интеграции с задачами (issues)

## Быстрый старт

```bash
# Добавьте ваш первый проект
session project add myproject /path/to/project

# Начните работу
session start myproject

# Ваша работа происходит здесь...

# Завершите сессию с сохранением контекста
session end

# Проверяйте статус в любое время
session status
```

## Основные команды

### Управление проектами

```bash
# Добавить новый проект
session project add <название> <путь> [--alias короткое_имя]

# Список всех проектов
session project list

# Удалить проект
session project remove <название>

# Получить информацию о проекте
session project info <название>
```

### Управление сессиями

```bash
# Начать сессию (автоопределение проекта или указание вручную)
session start [проект] [--description "Работа над функцией X"]

# Завершить сессию с сохранением контекста
session end [проект]

# Проверить текущий статус
session status [проект]

# Просмотреть историю сессий
session history [проект] [--limit 10]
```

## Что происходит во время сессии?

### При `session start`:
-   📖 Показывает ваш последний сохраненный контекст (следующие шаги из PROJECT.md)
-   🌿 Отображает текущую ветку Git и статус (если доступно)
-   📋 Список открытых задач GitHub (если доступен gh CLI)
-   🧪 Запускает тесты для проверки состояния проекта (если доступен pytest)
-   ⏰ Записывает время начала сессии

### При `session end`:
-   ✍️ Спрашивает, что вам удалось сделать
-   🎯 Спрашивает, какое следующее конкретное действие должно быть
-   💾 Создает снимок контекста в Markdown
-   📝 Обновляет PROJECT.md с разделом "Следующее действие"
-   🔀 Предлагает создать коммит Git, если есть изменения
-   📊 Показывает статистику по сессии

## Функции

### 🎯 Уникальные возможности

1.  **Снимки контекста (Context Snapshots)**: Каждое завершение сессии создает детализированный снимок
    -   Что было сделано
    -   Что дальше
    -   Состояние Git
    -   Статус тестов
    -   Незакоммиченные изменения

2.  **Автогенерация PROJECT.md**: Всегда знайте, с чего начать
    -   Резюме последней сессии
    -   Следующее конкретное действие
    -   Быстрые команды

3.  **Умная интеграция**: Работает с вашими инструментами
    -   Git: отслеживание веток, предложения коммитов, статус
    -   pytest: автоматический запуск тестов
    -   GitHub: отображение открытых задач (через gh CLI)

4.  **Поэтапная деградация (Graceful Degradation)**: Работает везде
    -   Нет git? Не проблема — все равно отслеживает время и контекст
    -   Нет pytest? Хорошо — пропускает статус тестов
    -   Нет gh CLI? OK — работает без задач GitHub

5.  **Локальный и приватный**: Все данные хранятся в `~/.session-manager/`
    -   Без облачной синхронизации (пока что)
    -   Полная конфиденциальность
    -   Быстро и просто

## Хранение данных

Все данные хранятся локально в вашей домашней директории:

```
~/.session-manager/
├── config.json              # Глобальная конфигурация
├── projects/                # Данные по проектам
│   ├── myproject/
│   │   ├── sessions.json    # История сессий
│   │   ├── PROJECT.md       # Автогенерируемый контекст
│   │   └── snapshots/       # Снимки контекста
│   │       ├── 20250114_1430.md
│   │       └── 20250114_1620.md
│   └── another-project/
└── logs/                    # Логи приложения (в будущем)
```

## Разработка

### Настройка среды разработки

```bash
# Клонируйте репозиторий
git clone https://github.com/Wwwoper/session-manager.git
cd session-manager

# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # В Windows: venv\Scripts\activate

# Установите в режиме разработки с dev-зависимостями
pip install -e ".[dev]"

# Запустите тесты
pytest

# Запустите тесты с покрытием
pytest --cov=session_manager --cov-report=html

# Форматирование кода
black session_manager tests

# Проверка типов
mypy session_manager
```

### Запуск тестов

```bash
# Запустить все тесты
pytest

# Запустить с измерением покрытия
pytest --cov=session_manager

# Запустить конкретный тестовый файл
pytest tests/test_core/test_session.py

# Запустить с подробным выводом
pytest -v
```

## План разработки (Roadmap)

### ✅ MVP (Текущая версия)
-   [x] Базовое управление проектами
-   [x] Отслеживание сессий с временем
-   [x] Снимки контекста
-   [x] Генерация PROJECT.md
-   [x] Интеграция с Git (опционально)
-   [x] Интеграция с pytest (опционально)
-   [x] Интеграция с GitHub Issues (опционально)

### 🔄 Следующие итерации
-   [ ] Цветной вывод в терминал (rich/colorama)
-   [ ] Интерактивный режим для удобства использования
-   [ ] Панель управления со статистикой
-   [ ] Поиск по истории сессий
-   [ ] Возможности экспорта (JSON, CSV, Markdown)
-   [ ] Шаблоны для распространенных рабочих процессов
-   [ ] Система плагинов

### 🚀 Будущее
-   [ ] Веб-панель управления (опциональный локальный сервер)
-   [ ] Интеграция с Jira/Linear
-   [ ] Командные функции (опциональная синхронизация)
-   [ ] Предложения на основе ИИ
-   [ ] Синхронизация между машинами (опционально)

## Почему Session Manager?

**Проблема**: Переключение между проектами — это сложно. Вы теряете контекст, забываете, что делали, и тратите время на повторное погружение.

**Решение**: Session Manager выступает в роли памяти вашего проекта:
-   Всегда знайте, что вы делали в последний раз
-   Всегда знайте, что делать дальше
-   Отслеживайте, сколько времени вы тратите
-   Поддерживайте свой контекст разработки свежим

**Отличие от других инструментов**:
-   Не просто трекер времени — это менеджер контекста
-   Не просто приложение для заметок — оно интегрировано в ваш рабочий процесс
-   Не просто менеджер задач — оно автоматическое и ненавязчивое
-   Работает локально — ваши данные остаются вашими

## Лицензия

MIT License — подробности см. в файле LICENSE.

## Участие в разработке

Вклад приветствуется! Не стесняйтесь отправлять Pull Request.

1.  Форкните репозиторий
2.  Создайте ветку для вашей функции (`git checkout -b feature/amazing-feature`)
3.  Закоммитьте ваши изменения (`git commit -m 'Add amazing feature'`)
4.  Запушьте в ветку (`git push origin feature/amazing-feature`)
5.  Откройте Pull Request

## Поддержка

-   📫 Задачи (Issues): [GitHub Issues](https://github.com/Wwwoper/session-manager/issues)
-   📖 Документация: [GitHub Wiki](https://github.com/Wwwoper/session-manager/wiki)

---

**Сделано с ❤️ для разработчиков, которые управляют множеством проектов**
