# TODO: Улучшение UX Session Manager CLI

> 📅 Дата аудита: 2026-04-09
> 📊 Статус: Ожидает реализации
> 🔍 Метод: Ручной аудит CLI-интерфейса и пользовательских сценариев

---

## 🔴 P0 — Критические (высокая польза, малые усилия)

### 1. Команда `session ls` — все активные сессии
- [ ] Реализовать команду `session ls` / `session list`
- [ ] Показывает все проекты с активными сессиями одновременно
- [ ] Для каждой сессии: проект, время начала, текущая длительность, описание
- [ ] Использует существующий `ProjectRegistry.get_projects_summary()` (уже считает `active_sessions`)
- **Файлы:** `cli/commands.py`
- **Затронутые строки:** `project_registry.py:256-280` (метод уже есть, нужно вывести в CLI)

### 2. Команда `session abort` — быстрое завершение без вопросов
- [ ] Реализовать команду `session abort [проект]`
- [ ] Завершает активную сессию без интерактивных вопросов
- [ ] Сохраняет сессию с пустыми `summary` и `next_action`
- [ ] Использует ту же логику поиска проекта, что и `session end`
- **Файлы:** `cli/commands.py`
- **Затронутые строки:** `commands.py:289-347` (логика `cmd_end` для рефакторинга)

### 3. Флаги `--force` / `-y` для неинтерактивного режима
- [ ] Добавить `--force` / `-y` к `session end` — пропускает вопросы, завершает с пустыми полями
- [ ] Добавить `--force` / `-y` к `session project remove` — удаляет без подтверждения
- [ ] Добавить `--force` / `-y` к `session start` — начинает без показа контекста (git, tests, github)
- **Файлы:** `cli/commands.py`
- **Затронутые строки:** `commands.py:199, 295-306` (промпты ввода)

---

## 🟡 P1 — Важные (высокая польза, средние усилия)

### 4. Флаг `--no-tests` / кэширование результатов тестов
- [ ] Добавить флаг `--no-tests` к `session start` и `session status`
- [ ] Кэшировать результаты последнего запуска тестов (показывать время кэширования)
- [ ] Добавить настройку в конфиг: `run_tests_on_start: true/false`
- **Файлы:** `cli/commands.py`, `core/config.py`
- **Затронутые строки:** `commands.py:634-647` (`_show_test_status`)

### 5. Подсказка `next_action` при `session end`
- [ ] При запросе "Следующее действие" показывать текущее значение из PROJECT.md
- [ ] Формат: `Следующее действие: [текущее_значение]`
- [ ] Пользователь может нажать Enter для сохранения текущего или ввести новое
- **Файлы:** `cli/commands.py`
- **Затронутые строки:** `commands.py:297-298` (промпт next_action)

### 6. Общее время за сегодня в `session status`
- [ ] Показывать `Всего за сегодня: Xч Yм` в выводе `session status`
- [ ] Использовать существующий метод `SessionManager.get_total_time_today()`
- **Файлы:** `cli/commands.py`
- **Затронутые строки:** `commands.py:372-414` (`cmd_status`)
- **Существующий метод:** `session.py:287-298` (`get_total_time_today`)

### 7. Предложение начать сессию при `session status` без активной
- [ ] Если нет активной сессии — добавить подсказку: `Начните сессию: session start`
- **Файлы:** `cli/commands.py`
- **Затронутые строки:** `commands.py:403-404` ("Нет активной сессии")

---

## 🟢 P2 — Средние (средняя польза, средние усилия)

### 8. Команда `session resume`
- [ ] Реализовать `session resume [проект]`
- [ ] Находит последнюю завершённую сессию
- [ ] Предлагает использовать `next_action` как описание новой сессии
- [ ] Показывает резюме предыдущей сессии для контекста
- [ ] После подтверждения — начинает новую сессию
- **Файлы:** `cli/commands.py`
- **Затронутые строки:** `session.py:117-134` (`cmd_end` — откуда берётся next_action)

### 9. Shell completion (bash/zsh/fish)
- [ ] Реализовать `session completion --shell <bash|zsh|fish>`
- [ ] Автодополнение: имена проектов, команды, флаги
- [ ] Инструкция по установке в README
- **Файлы:** `cli/commands.py`, `README.md`
- **Примечание:** CLI не использует argparse/click, потребуется ручная реализация

### 10. Команда `session edit <id>`
- [ ] Редактирование завершённой сессии: summary, next_action, description
- [ ] Использует существующий `SessionManager.update_session_metadata()`
- [ ] Добавить новые поля: `summary`, `next_action`, `description`
- **Файлы:** `cli/commands.py`, `core/session.py`
- **Затронутые строки:** `session.py:248-273` (`update_session_metadata`)

### 11. Режимы `--verbose` / `--quiet`
- [ ] `--verbose` — полный текст резюме, полный git diff, без обрезки
- [ ] `--quiet` / `-q` — минимум вывода, только ключевая информация
- [ ] Применить к: `session history`, `session status`, `session end`
- **Файлы:** `cli/commands.py`, `utils/formatters.py`
- **Затронутые строки:** `commands.py:478-483` (обрезка 60 символов)

### 12. Расширенный показ git diff при `session end`
- [ ] Показывать список изменённых файлов (не только статус)
- [ ] Увеличить лимит или показать полностью
- [ ] Опция: `session end --diff` — показать полный diff
- **Файлы:** `cli/commands.py`, `integrations/git.py`
- **Затронутые строки:** `commands.py:309-310` (`print(changes[:200])`)

---

## 🔵 P3 — Технические (низкая пользовательская польза, важны для поддержки)

### 13. Выделение общего fallback-сервиса
- [ ] Создать единый класс/метод для разрешения проекта
- [ ] Убрать дублирование между `cmd_status`, `cmd_history`, `cmd_stats`, `_find_active_session`
- [ ] Единая точка настройки приоритетов: active → current → auto-detect
- **Файлы:** `cli/commands.py`
- **Затронутые строки:** `commands.py:386-416, 456-494, 519-554, 561-579`

### 14. Валидация путей при загрузке
- [ ] При `session project list` предупреждать о проектах с несуществующими путями
- [ ] Вызывать `ProjectRegistry.validate_all()` при загрузке
- [ ] Показывать ⚠️ рядом с "битыми" проектами
- **Файлы:** `cli/commands.py`, `core/project_registry.py`
- **Затронутые строки:** `project_registry.py:237-252` (`validate_all` — метод уже есть, не используется в CLI)

### 15. Настройки пользователя в GlobalConfig
- [ ] Добавить секцию `settings` в config.json
- [ ] Поля: `default_history_limit`, `run_tests_on_start`, `check_github_issues`, `timeout_tests`, `output_verbosity`
- [ ] Команда `session config show` / `session config set <key> <value>`
- **Файлы:** `core/config.py`, `cli/commands.py`

### 16. Блокировка файлов (race condition prevention)
- [ ] Добавить file locking при записи `sessions.json`
- [ ] Предотвращает гонку при одновременном `session start` из двух терминалов
- **Файлы:** `core/storage.py`, `core/session.py`
- **Затронутые строки:** `session.py:62-91` (`start()` — проверка и запись не атомарны)

---

## 📋 СВОДНАЯ ТАБЛИЦА

| # | Задача | Приоритет | Усилия | Польза | Статус |
|---|--------|-----------|--------|--------|--------|
| 1 | `session ls` — все активные сессии | 🔴 P0 | Малые | Высокая | ⬜ Ожидает |
| 2 | `session abort` — быстрое завершение | 🔴 P0 | Малые | Высокая | ⬜ Ожидает |
| 3 | Флаги `--force`/`-y` | 🔴 P0 | Малые | Высокая | ⬜ Ожидает |
| 4 | `--no-tests` / кэширование тестов | 🟡 P1 | Средние | Высокая | ⬜ Ожидает |
| 5 | Подсказка `next_action` при `end` | 🟡 P1 | Малые | Средняя | ⬜ Ожидает |
| 6 | Время за сегодня в `status` | 🟡 P1 | Малые | Средняя | ⬜ Ожидает |
| 7 | Подсказка "начните сессию" | 🟡 P1 | Малые | Средняя | ⬜ Ожидает |
| 8 | `session resume` | 🟢 P2 | Средние | Средняя | ⬜ Ожидает |
| 9 | Shell completion | 🟢 P2 | Средние | Средняя | ⬜ Ожидает |
| 10 | `session edit <id>` | 🟢 P2 | Средние | Средняя | ⬜ Ожидает |
| 11 | `--verbose` / `--quiet` | 🟢 P2 | Средние | Низкая | ⬜ Ожидает |
| 12 | Расширенный git diff | 🟢 P2 | Малые | Средняя | ⬜ Ожидает |
| 13 | Общий fallback-сервис | 🔵 P3 | Средние | Техн. | ⬜ Ожидает |
| 14 | Валидация путей | 🔵 P3 | Малые | Техн. | ⬜ Ожидает |
| 15 | Настройки пользователя | 🔵 P3 | Средние | Техн. | ⬜ Ожидает |
| 16 | File locking (race condition) | 🔵 P3 | Средние | Техн. | ⬜ Ожидает |

---

## 🗒️ ЗАМЕЧАНИЯ

- Все команды должны поддерживать ту же логику разрешения проекта: **active session → current_project → auto-detect**
- Новые команды должны быть полностью русифицированы (комментарии, docstrings, пользовательский вывод)
- Покрытие тестами ≥ 70% для всех новых функций
- Обратная совместимость: существующие команды не должны менять поведение без флагов
